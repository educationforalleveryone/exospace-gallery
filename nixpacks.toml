[phases.setup]
nixPkgs = ["php", "phpExtensions.redis", "nginx", "nodejs", "phpPackages.composer"]

[phases.build]
cmds = [
    "npm install",
    "npm run build",
    "composer install --no-dev --optimize-autoloader"
]

[start]
cmd = "bash docker-start.sh"