[phases.setup]
# Removed "phpPackages.imagick" — the pinned nixpkgs archive
# (e24b4c09e963677b1beea49d411cd315a024ad3a) no longer ships it,
# which broke the Docker image build at step 4/18 ("attribute 'imagick' missing").
# Image processing in this app uses GD (already enabled via
# NIXPACKS_PHP_EXTENSIONS=gd in .env), so imagick is not required.
nixPkgs = ["php", "nginx", "nodejs", "phpPackages.composer"]

[phases.build]
cmds = [
    "npm install",
    "npm run build",
    "composer install --no-dev --optimize-autoloader",
    # TD-4: Build caches at deploy time so the container starts fast.
    # Previously, docker-start.sh was clearing caches on every boot (TD-2).
    # Now caches are built here (deploy time) and docker-start.sh just
    # runs storage:link + preflight.
    "php artisan config:cache",
    "php artisan route:cache",
    "php artisan view:cache"
]

[start]
cmd = "bash docker-start.sh"
